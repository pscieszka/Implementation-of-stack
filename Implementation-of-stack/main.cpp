#include <stdio.h>
#include <stdlib.h>
/*
#include <string.h>
#include "student.h"
#include "message.h"
#include "stack.h"
*/
#include "interface.h"
int main(){

    menu();
















	/*student s;
	char nazwisko[100];
	int wiek;
	kierunek kierunekStudiow;

	printf("Podaj nazwisko studenta: ");
	scanf_s("%s", nazwisko, 100);

	wiek = studentAge();

	printf("Podaj kierunek studiow (0-Informatyka, 1-Matematyka, 2-Fizyka): ");
	scanf_s("%d", &kierunekStudiow);

	if (studentInit(&s, wiek, nazwisko, kierunekStudiow))
	{
		printf("Utworzono obiekt student:\n");
		studentPrint(&s);
		studentFree(&s);
	}
	else
	{
		printf("Nie udalo sie utworzyc obiektu student\n");
	}
	
    Stack stack;
    stack_init(&stack);

    // Przyk³adowe dane studenta
    student s1;
    s1.nazwisko = "es";
    s1.wiek = 20;
    s1.kierunekStudiow = INFORMATYKA;

    student s2;
    s2.nazwisko = "Es232";
    s2.wiek = 22;
    s2.kierunekStudiow = MATEMATYKA;

    // Dodanie studentów na stos
    stack_push(&stack, s1);
    stack_push(&stack, s2);

    // Pobranie i wyœwietlenie studentów ze stosu
    student poppedStudent1 = stack_pop(&stack);
    printf("Pobrany student 1:\n");
    printf("Nazwisko: %s\n", poppedStudent1.nazwisko);
    printf("Wiek: %d\n", poppedStudent1.wiek);
    printf("Kierunek studiow: %d\n", poppedStudent1.kierunekStudiow);

    student poppedStudent2 = stack_pop(&stack);
    printf("Pobrany student 2:\n");
    printf("Nazwisko: %s\n", poppedStudent2.nazwisko);
    printf("Wiek: %d\n", poppedStudent2.wiek);
    printf("Kierunek studiow: %d\n", poppedStudent2.kierunekStudiow);

    // Zwolnienie pamiêci zajmowanej przez stos
    stack_free(&stack);*/
	return 0;
}


